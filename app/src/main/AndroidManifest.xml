<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FocusZone">
        <activity
            android:name=".ui.timer.TimerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.configuracao.ConfiguracaoActivity"
            android:exported="false"
            android:parentActivityName=".ui.timer.TimerActivity"/>
        <activity
            android:name=".ui.historico.HistoricoActivity"
            android:exported="false"
            android:parentActivityName=".ui.timer.TimerActivity"/>
        <activity
            android:name=".ui.tarefas.TarefasActivity"
            android:exported="false"
            android:parentActivityName=".ui.timer.TimerActivity"/>

        <!-- TODO: adicione android:stopWithTask="false" neste <service> — ver exercicios/aula-08/enunciado.md -->
        <service
            android:name=".service.TimerService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="focus_timer" />
        </service>
    </application>

</manifest>
